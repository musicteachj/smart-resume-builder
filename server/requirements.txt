# Exact pins (== the versions verified by the test suite) so image builds are
# reproducible and upstream releases can't land silently. Upgrade deliberately:
# bump a pin, run the suite, commit.

# Web framework
Django==5.2.15
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
drf-spectacular==0.29.0
django-cors-headers==4.9.0

# Database
psycopg[binary]==3.3.4
dj-database-url==3.1.2

# Config / env
python-dotenv==1.2.2

# Static serving (prod container)
whitenoise==6.12.0
gunicorn==26.0.0

# AI (Phase 7)
anthropic==0.109.1

# Testing
pytest==9.0.3
pytest-django==4.12.0
